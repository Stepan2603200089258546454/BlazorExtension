using CommonComponents.Enums.Exstension;

namespace CommonComponents.Enums.Bootstraps
{
    public enum AccordionType
    {
        [StyleValue]
        Default = 0,
        [StyleValue("accordion-flush")]
        Flush = 1,
    }
}
